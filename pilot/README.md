# 🚗 АвтоИмпорт Pro — Пилотный ИИ-агент

Пилотный проект ИИ-ассистента для автоматизации первичного контакта с клиентами в сфере импорта автомобилей.

## 🎯 Что делает система

1. **Лендинг** — красивый сайт компании с информацией об услугах
2. **Чат-виджет** — интерактивный чат с ИИ-консультантом
3. **ИИ-агент** — LangGraph агент, который:
   - Приветствует клиента
   - Выясняет потребности (марка, модель, бюджет, сроки)
   - Отвечает на вопросы из базы знаний
   - Квалифицирует лид (горячий/тёплый/холодный)
   - Собирает контактные данные
4. **Админ-панель** — просмотр лидов, диалогов, статистики

## 🚀 Быстрый старт

### 1. Установка зависимостей

**Backend (Python 3.11+):**
```bash
cd pilot/backend
pip install -r requirements.txt
```

**Frontend (Node.js 18+):**
```bash
cd pilot/frontend
npm install
```

### 2. Настройка OpenAI API

Создайте файл `pilot/backend/.env`:
```
OPENAI_API_KEY=sk-your-api-key-here
```

### 3. Запуск

**Терминал 1 — Backend:**
```bash
cd pilot/backend
python main.py
```
Сервер запустится на http://localhost:8000

**Терминал 2 — Frontend:**
```bash
cd pilot/frontend
npm run dev
```
Сайт откроется на http://localhost:5173

## 📁 Структура проекта

```
pilot/
├── backend/
│   ├── main.py           # FastAPI сервер
│   ├── agent.py          # LangGraph ИИ-агент
│   ├── database.py       # SQLite модели (лиды, диалоги)
│   ├── knowledge_base.py # FAQ и база знаний
│   └── requirements.txt
│
└── frontend/
    ├── src/
    │   ├── App.tsx
    │   └── components/
    │       ├── Landing.tsx      # Лендинг
    │       ├── ChatWidget.tsx   # Чат-виджет
    │       └── AdminPanel.tsx   # Админ-панель
    └── package.json
```

## 🎮 Демо-сценарии

### Сценарий 1: Горячий лид
```
Клиент: Хочу Hyundai Tucson из Кореи, бюджет 2.5 млн, нужно срочно
→ Агент собирает данные, квалифицирует как "горячий", запрашивает контакты
```

### Сценарий 2: Исследование
```
Клиент: Сколько стоит привезти машину из Японии?
→ Агент отвечает из базы знаний, уточняет потребности
```

### Сценарий 3: Сравнение
```
Клиент: Что лучше — Корея или Япония?
→ Агент консультирует, помогает определиться
```

## 🔧 API Endpoints

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/chat` | Отправка сообщения агенту |
| GET | `/api/leads` | Список всех лидов |
| GET | `/api/leads/{session_id}` | Данные конкретного лида |
| GET | `/api/conversations/{session_id}` | История диалога |
| GET | `/api/stats` | Статистика по лидам |

## 📊 Метрики агента

Агент извлекает следующие параметры (slot-filling):
- `car_brand` — марка авто
- `car_model` — модель
- `budget_min/max` — бюджет
- `country` — страна-источник
- `timeline` — сроки покупки
- `body_type` — тип кузова
- `name` — имя клиента
- `phone` — телефон

Квалификация лида:
- 🔥 **Горячий** — бюджет >3 млн или срочная покупка + контакты
- 🌡️ **Тёплый** — есть базовые параметры + контакты
- ❄️ **Холодный** — мало информации

## 🛠 Технологии

- **Frontend:** React, TypeScript, Tailwind CSS, Vite
- **Backend:** FastAPI, LangGraph, LangChain, OpenAI GPT-4o-mini
- **Database:** SQLite (aiosqlite)
- **Fonts:** Unbounded, Onest

---

*Пилотный проект для демонстрации возможностей AIOps Framework*
